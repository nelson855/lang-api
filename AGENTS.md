# AGENTS.md

## 1. 文档目的

- 本文件用于约束 OpenAI Codex 在本项目中的工作方式、代码修改方式、验证方式与协作方式。
- 本文件只保留项目级的必要约束与规范入口，详细代码风格规范与编写更新指引见 `docs/agent-guidelines/` 下的配套附件。
- 本文件的目标是减少无效修改、降低沟通成本，并提升代码质量与可维护性。

## 2. 使用范围

- 本文件默认作用于当前仓库根目录及其所有子目录。
- 如果子目录存在更细粒度的 `AGENTS.md`，则以更靠近目标文件的那份说明为准。
- 系统指令、平台指令与用户明确提出的要求，优先级高于本文件。

## 3. 文档语言约束

- 本项目默认使用中文编写协作文档、任务清单、变更说明与测试说明。
- 代码标识符、API 路径、字段名、枚举值、命令、错误码、库名等保持原文英文。
- 如外部工具模板为英文，生成内容时也应优先翻译为中文，只保留必要的模板关键字或规范性标题。
- 若用户没有特别要求英文，最终回复与阶段性说明也默认使用中文。

## 4. 项目概览

## 5. 仓库结构

## 6. 开发命令

## 7. 测试与验证

## 8. 编码规范

### 8.1 Andrej Karpathy 代码规范约束

- 本项目采用 Andrej Karpathy 风格的代码规范约束，用于减少大模型在编程过程中的常见失误，例如过早抽象、无关改动、跳过澄清、缺少验证等问题。
- 这套规范的目的，是要求大模型在编程时遵循“先思考、保持简单、外科手术式修改、目标驱动执行”的工作方式，以提升代码修改质量与可维护性。
- 四条核心原则：
    1. **编码前先思考**：不要假设，不要掩饰困惑，明确呈现权衡；不清楚的就停下来提问。
    2. **简单优先**：只写解决问题所需的最少代码；不做预设性扩展，不为一次性代码做抽象。
    3. **外科手术式修改**：只改必须改的内容；保持现有风格；每一行改动都应能直接追溯到用户请求。
    4. **目标驱动执行**：先定义可验证的成功标准，再循环推进；用测试 / 验证步骤闭环。
- 详细规范见：[Andrej Karpathy 代码规范约束](docs/agent-guidelines/Andrej%20Karpathy%20代码规范约束.md)
- 配套示例见：[Andrej Karpathy 代码规范示例](docs/agent-guidelines/Andrej%20Karpathy%20代码规范示例.md)

### 8.2 项目特定编码习惯

<!--
在这一节补充项目特定的编码习惯与个人偏好，例如：
- 语言版本、框架版本、运行时
- 命名规则、目录命名规则
- 错误处理与日志风格
- 依赖引入规则、工具函数复用规则
保持简洁，与 Karpathy 规范冲突时以本节为准；本节为空时完全沿用上一节。
-->

### 8.3 项目结构划分规范

- 目录结构采用朴素的传统技术分层，不按业务边界拆分大包。
- HTTP/API 入参与出参从 Controller 中拆出，放到 request/response、dto 或项目已有等价目录。
- 数据库表对象、查询投影对象、接口 DTO 要分清；可使用 po、projection、dto 等命名。
- 枚举、常量、异常等基础类型不要混在业务服务或数据模型里，可放到类似 base/constants、base/exception 的基础目录。
- 存在接口与实现时，实现类收敛到同一职责包下的 impl 或项目已有等价目录。
- 后台任务、定时任务、消费者工作线程等运行单元从业务服务中分离；目录名可按项目语义选择，例如 worker、job、task、scheduler。
- 不要机械抽 service 接口；只有出现多实现、跨模块边界、外部扩展点或测试隔离确有需要时再抽。

## 9. 架构规则

## 10. 工作流规则

## 11. 安全与禁止事项

## 12. 完成标准

## 13. 常见坑

## 14. 本文件的更新方式

- 当项目结构、开发命令、测试方式、架构边界、常见坑或安全约束发生变化时，应同步更新本文件。
- 更新本文件时，请优先依据：[AGENTS 编写更新指引](docs/agent-guidelines/AGENTS编写更新指引.md)
- 不要机械套用指引中的全部章节；应结合当前仓库实际情况，只保留真正有用、可执行、可验证的内容。
- 如果某些信息暂时无法确认，请写 `TODO` 并说明需要补充什么，不要凭空编造。
- **章节长度自动抽取规则**：当某个章节（含子节）正文超过 **80 行** 时，应将该章节抽取为独立文件 `docs/agent-guidelines/sections/{章节名}.md`，并在本文件中保留章节标题与一行链接引用。抽取后主文件该章节只保留：标题 + 一两句概述 + 指向独立文件的链接。
