## 1. 探测与证据安全基础

- [x] 1.1 先为 opt-in 开关、本地隔离地址限制、凭证仅从环境读取、原始输出只进入 `target/` 编写测试，再实现第二阶段 New API 实测入口，确保默认 Maven 测试不访问网络
- [x] 1.2 先为字段白名单、一致占位符、关联关系保留和映射不落盘编写测试，再实现日志、账户、充值和价格响应的脱敏转换
- [x] 1.3 先为 Cookie/Header、Authorization/Bearer、完整 Key、邮箱、手机号、IP、请求内容、渠道和供应商密钥等敏感模式编写拒绝测试，再实现固化前安全扫描
- [x] 1.4 定义并测试 `docs/new-api/aggregation-baseline.json` 的必填结构、四态结论、唯一标识、证据引用和版本一致性校验
- [x] 1.5 定义并测试 `docs/new-api/samples/aggregation/manifest.json` 的场景、冻结版本、实测时间、环境摘要和文件摘要规则，确认悬空或被篡改的 fixture 会使验证失败

## 2. 隔离环境与验证数据集

- [ ] 2.1 核对冻结版 New API release、commit、manifest/platform digest、Compose 配置和测试日期，并记录第二阶段探测环境、机器资源与网络位置
- [ ] 2.2 编写可重复的数据准备说明，通过公开或既有允许流程准备空账户、两个以上 Key、成功非流式/流式请求、重复 Key 请求和可控上游失败
- [ ] 2.3 准备并记录跨 UTC 日界、相邻区间和夏令时自然日所需的时间样例，确认不通过修改 New API 数据库伪造证据
- [ ] 2.4 尝试准备充值成功、处理中/失败和退款数据，以及多厂商、多计费类型价格数据；外部条件不足的场景记录缺失条件并保持 `条件可用` 或 `不可用`

## 3. 六接口真实探测与脱敏样例

- [ ] 3.1 实测 `/api/log/self` 的非空、空数据、未认证、非法分页/筛选、时间边界和日志类型场景，固化通过安全扫描的脱敏 fixture
- [ ] 3.2 实测 `/api/log/self/stat` 的非空、空数据、未认证、非法时间和相邻区间场景，固化通过安全扫描的脱敏 fixture
- [ ] 3.3 实测 `/api/data/self` 的非空、空数据、未认证、非法时间、小时桶边界和数据延迟场景，固化通过安全扫描的脱敏 fixture
- [ ] 3.4 实测 `/api/user/self` 的有效会话、未认证及余额/累计字段边界，固化通过安全扫描的脱敏 fixture
- [ ] 3.5 实测 `/api/user/topup/self` 的非空或可用数据、空数据、未认证、非法分页和各可构造状态，固化通过安全扫描的脱敏 fixture
- [ ] 3.6 实测 `/api/pricing` 的匿名/认证、非空、空或禁用配置、多厂商与多计费类型场景，固化通过安全扫描的脱敏 fixture
- [ ] 3.7 完成 manifest 和 fixture 离线契约测试，确认所有已验证场景都有可解析证据且仓库中没有原始响应或凭证映射

## 4. 字段来源与兼容性冻结

- [ ] 4.1 创建 `docs/new-api/第二阶段聚合数据兼容性矩阵.md`，逐接口记录认证、参数、分页、时间语义、场景证据、前置条件、实测日期和四态结论
- [ ] 4.2 在机器基线中逐字段记录 JSON 路径、类型、单位、可空性、适用日志类型和样例引用，覆盖状态、Token、费用、耗时、Key 标识、协议、TTFT、充值、退款和模型价格字段
- [ ] 4.3 对同一概念的多来源执行抽样对账，冻结唯一权威来源、校验来源、上游时效差异和冲突处理规则
- [ ] 4.4 将未验证、语义矛盾或外部条件缺失的字段明确标记为 `存在差异`、`条件可用` 或 `不可用`，并增加测试阻止其被声明为确定支持

## 5. 指标、流水、时间与金额口径

- [ ] 5.1 基于实测证据冻结请求总数、Token 用量、消费总额、活跃 Key、成功率和平均延迟的来源、分子分母、纳入排除、去重、单位、异常/缺失和不可用规则
- [ ] 5.2 用脱敏样例为六项指标建立预期结果测试，并对请求数、Token 与消费额至少各完成一次独立来源抽样对账
- [ ] 5.3 冻结充值、消费和退款的来源、方向、状态、发生时间、稳定 ID、公开引用和去重规则；无法确认的退款能力保持不可用且不得按金额正负推断
- [ ] 5.4 实测并冻结上游时间单位与包含性、Portal `[startTime,endTime)` 转换、UTC 运算、IANA 自然日、允许粒度、空桶策略和最大跨度组合
- [ ] 5.5 对日志、统计、余额、充值和价格字段核对 quota/币种单位，冻结 `BigDecimal` 累计、`quota-per-usd` 换算、舍入阶段和未知单位处理
- [ ] 5.6 创建 `docs/new-api/第二阶段统计与流水口径.md`，引用同一机器基线版本并明确下游 DTO 对 `null`、未知状态和不可用指标的处理

## 6. 性能测量与保护边界

- [ ] 6.1 为性能探测增加调用次数、分页数、记录数、响应字节、耗时和错误记录，并验证测量结果不包含响应正文或凭证
- [ ] 6.2 对空/小数据用户和典型数据量用户分别测量 24 小时、7 天、30 天范围，执行预热、冷启动和默认 20 次稳定测量
- [ ] 6.3 计算并复核 p50、p95 和最大值，记录硬件、容器资源、数据规模、网络与 New API 配置；历史数据不足时明确证据限制且不线性外推
- [ ] 6.4 创建 `docs/new-api/第二阶段聚合性能基线.md`，冻结供 LANG-P2-02 使用的最大页数、最大记录数、总超时和范围/粒度保护边界，或记录需要单独评估预聚合的触发证据

## 7. 基线发布与下游门禁

- [ ] 7.1 发布统一 `baselineVersion`，确保机器基线、兼容性矩阵、统计流水口径、性能报告、manifest 和全部 fixture 引用同一冻结版身份
- [ ] 7.2 扩展 `docs/new-api/第一阶段接口兼容性矩阵.md` 或其索引，链接第二阶段矩阵并说明上游升级时必须重跑字段、口径和性能验证
- [ ] 7.3 增加基线完整性测试，阻止缺失字段语义、非法状态、重复 ID、悬空证据、版本漂移或把条件/不可用字段声明为下游确定值
- [ ] 7.4 更新 `docs/11_第二阶段聚合能力开发与子需求拆分.md` 中 LANG-P2-01 的实际状态和完成结果；只有全部必需证据与验证完成后才标记为 `已完成`

## 8. 验证与交付记录

- [ ] 8.1 使用项目指定的 Maven 与 `settings.xml` 运行新增单元、fixture 契约和敏感数据检查，再运行根工程 `verify`
- [ ] 8.2 按冻结公式对六项指标、至少一种流水和时间/金额边界执行人工抽样复算，记录与机器基线一致或不可用的证据
- [ ] 8.3 运行 OpenSpec 严格校验和文档/JSON 引用完整性检查，确认 proposal、spec、design、tasks 与交付基线一致
- [ ] 8.4 运行 `git diff --check` 和 `git status`，确认没有原始响应、凭证、占位任务或未经验证却已勾选的任务，并报告所有未提交改动
