## 1. 配置与基线门禁

- [ ] 1.1 先为聚合配置的默认值、正值约束、页数/记录数关系、单次/总超时关系、粒度跨度和未知基线版本编写绑定与启动失败测试，再扩展 `PortalCommonProperties` 与配置校验器
- [ ] 1.2 在 `application.properties` 中加入 `p2-2026-09-22-a`、20 条分页、10 页/200 条、5 秒/30 秒、7 天实时日志范围及 30 秒/1,000 条缓存默认值，并在 `application-dev.properties`、`application-test.properties`、`application-prod.properties` 中显式声明环境值或受控覆盖方式
- [ ] 1.3 增加 `AggregationBaselinePolicy` 的版本与字段/指标四态契约测试，再实现代码内支持矩阵，确保当前请求总数、成功率和 USD 消费金额不能被当作已验证值

## 2. 时间上下文与分桶

- [ ] 2.1 先为必填起止时间、带偏移量 ISO 8601、秒级精度、`start < end`、IANA 时区、稳定粒度和跨度/粒度组合编写失败与成功测试，再实现不可变 `AggregationQueryContext` 及解析策略
- [ ] 2.2 先为 `[startTime,endTime)`、上游包含式结束秒转换和相邻区间边界编写测试，再实现统一时间边界转换，避免各数据源自行执行 `end - 1s`
- [ ] 2.3 先为 `FIVE_MINUTES`、`HOUR`、`DAY` 的完整覆盖、首尾裁剪桶和空桶补齐编写测试，再实现 `AggregationBucketPlan`
- [ ] 2.4 增加至少一个 IANA 夏令时开始日和结束日测试，验证 23/25 小时自然日及重复本地小时使用不同 UTC 边界

## 3. 日志字段规范化与安全累计

- [ ] 3.1 先扩展真实 fixture 契约和 mapper 测试以覆盖 `token_id` 的存在、缺失与非法类型，再更新 New API 日志 raw、page 和 mapper 类型且保持既有日志接口兼容
- [ ] 3.2 先为已验证字段、缺失字段和条件可用字段编写映射测试，再实现不暴露 New API DTO 的 `AggregationLogRecord` 与转换器
- [ ] 3.3 先为 Token/耗时精确加法溢出、`BigDecimal` 原始 quota 累计、未知金额单位和禁止提前舍入编写测试，再实现可复用累计组件
- [ ] 3.4 增加契约测试，确认 `quotaPerUsd` 条件未满足时不调用现有默认换算生成正式 USD 值，type=5 未冻结时也不生成确定成功率

## 4. 受控分页与共享查询预算

- [ ] 4.1 先用伪分页源为完整单页/多页、首页总数超限、累计页数/记录数超限、总数变化、重复页、提前空页和异常短页编写失败测试
- [ ] 4.2 实现同步 `AggregationReadBudget` 与通用受控分页协调器，使同一业务聚合的多个数据源共享页数、记录数和总截止时间
- [ ] 4.3 先为调用前后截止时间、单次 HTTP 超时与总预算关系编写测试，再将超时分别映射为安全的 `INVALID_ARGUMENT`、`UPSTREAM_TIMEOUT` 或 `UPSTREAM_ERROR` 语义
- [ ] 4.4 将受控读取器接入 `NewApiLogClient` 单页函数，增加 MockWebServer 契约测试，验证固定 endTime、页码推进、范围外记录拒绝和失败时不返回部分结果
- [ ] 4.5 增加成功日志与错误日志共用一份预算的测试，确认任一来源耗尽预算时整次聚合失败

## 5. 本地缓存与同键并发合并

- [ ] 5.1 先为操作、用户、UTC 范围、粒度、IANA 时区、口径版本和排序后筛选条件的键差异编写测试，并验证键不接受会话、Cookie、完整 API Key 或上游原始对象
- [ ] 5.2 实现只缓存 Portal 自有不可变投影的 Caffeine 缓存，应用配置 TTL 和最大容量
- [ ] 5.3 增加同键并发未命中测试，确认只执行一次上游加载且等待者复用结果；增加跨用户、跨时区、跨口径和跨筛选条件不复用测试
- [ ] 5.4 增加异常、超时、取消和保护性拒绝测试，确认失败值与并发占位被清理、后续同键请求能够重新加载

## 6. 观测与敏感信息约束

- [ ] 6.1 先为聚合耗时、上游调用/页数/记录数、缓存 hit/miss/coalesced 和保护拒绝原因编写 MeterRegistry 测试，再实现低基数 Micrometer 观测组件
- [ ] 6.2 增加指标 tag 白名单与日志捕获测试，确认用户 ID、时间范围、模型 ID、request ID、API Key、会话和原始异常文本不进入指标标签，认证信息和上游正文不进入日志
- [ ] 6.3 将当前 requestId 仅接入安全结构化警告日志，覆盖范围、页数、记录数、截止时间和分页不一致等有限拒绝原因

## 7. 纵向集成与架构约束

- [ ] 7.1 建立不暴露新公开路由的纵向测试夹具，覆盖“查询解析 → 共享预算分页 → Portal 记录 → 分桶/累计 → 缓存复用”的成功链与保护失败链
- [ ] 7.2 扩展架构测试，禁止 `base/aggregation` 依赖 Web 或 New API DTO，并禁止未来业务查询服务直接向公开 DTO 返回 `upstream.newapi` 类型
- [ ] 7.3 使用 `p2-2026-09-22-a` 的脱敏 fixture 复算已验证 Token、活跃 Key 和平均延迟；对请求总数、成功率与 USD 金额断言明确不可用

## 8. 验证与交付记录

- [ ] 8.1 使用 `/Users/nelson/software/apache-maven-3.8.4/bin/mvn -s /Users/nelson/software/apache-maven-3.8.4/conf/settings.xml` 先运行 `portal-api` 相关测试，再运行当前环境可执行的根工程 `verify`，如有既有或环境阻塞需保留证据并明确区分
- [ ] 8.2 运行 OpenSpec 严格校验、配置隔离检查、敏感信息检查和 `git diff --check`，确认 proposal、spec、design、tasks 与实现及 `p2-2026-09-22-a` 一致
- [ ] 8.3 仅在全部适用验收通过后更新 `docs/11_第二阶段聚合能力开发与子需求拆分.md` 中 LANG-P2-02 的状态、实测保护边界和未解决外部条件；未验证项保持未完成或受阻
- [ ] 8.4 运行 `git status` 并报告所有未提交改动、测试结果、保护性限制和 P2-01 仍未闭合的证据，不执行提交、推送、发布或部署
